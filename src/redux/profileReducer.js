
let initialState = {
    isAuth: false,
    isLoadApp: false,
    profile: null
}

let profileReducer = (state = initialState, action) => {
    switch(action.type){
        
    }
    return state;
}

export default profileReducer;